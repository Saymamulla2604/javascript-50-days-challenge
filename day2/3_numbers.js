"use strict";
let amount=400;
console.log(amount.toFixed(2));//used in ecommerse for precision

amount=23.56789;
console.log(amount.toPrecision(6));//converts to precise value

const pkr=100000000;
console.log(pkr.toLocaleString()); //converts number into string with readable format
